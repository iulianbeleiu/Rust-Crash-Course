# Rust Crash Course | Rustlang

```
# Install Rust
https://www.rust-lang.org/
Use default installation, it will install also Cargo which is the dependency manager

# Create new project in current folder
cargo init

# Compile and run
cargo run

# Build
cargo build

# Build for production
cargo build --release
```